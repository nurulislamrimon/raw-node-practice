{
  "name": "core_practice",
  "version": "1.0.0",
  "description": "this is a practice project of core concept Node and js",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env env:NODE_ENV=development ts-node-dev --respawn --transpile-only src/index.ts",
    "start": "NODE_ENV=production node dist/index.js"
  },
  "author": "Nurul Islam Rimon",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "cross-env": "^7.0.3",
    "ts-node-dev": "^2.0.0"
  }
}
